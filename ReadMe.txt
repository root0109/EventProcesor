1)Make sure you have gradle installed. on ubuntu sudo-apt-get install gradle
2) on terminal go to 

cd <project workspace>/EventProcessor

3) run the below command to build

gradle war

4) It will Build Succesfully

5) deploy the war file present in build/target/EventProcessor.war in the tomcat webapps directory